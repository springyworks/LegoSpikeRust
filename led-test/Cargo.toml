[package]
name = "led-test"
version = "0.1.0"
edition = "2021"

[dependencies]
cortex-m = { version = "0.7", features = ["critical-section-single-core"] }
cortex-m-rt = "0.7"

[profile.dev]
opt-level = "s"
panic = "abort"

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
